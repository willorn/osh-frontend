# Vue / Nuxt 约定

- 页面文件只做页面编排。
- 复杂业务逻辑提到 composables。
- 组件 props 要清楚，避免隐式依赖全局状态。
- middleware 只处理路由层逻辑。
- 不在页面散写重复请求。
- Nuxt 配置变更必须说明影响。

## 路由

- 保持已有核心路由兼容。
- 改路由必须提供跳转兼容策略。
- 登录跳转要保留 `from`。

## 渲染

当前 `ssr: false`。如果要开启 SSR，必须先建立 OpenSpec change，评估登录态、浏览器 API、构建产物和部署影响。
